"""Streamlit dashboard for the India lead mass-balance v5 parallel chain.

Modules:
  state         — session-state helpers + the canonical chain solve
  controls      — Tab 2 inputs (stock, segments, k, tau, phi)
  sidebar       — sidebar inputs (eta, beta, gamma)
  diagram       — Tab 1 flow diagram (streamlit-flow-component) + line graph
  readme        — Tab 3 readme renderer
"""
